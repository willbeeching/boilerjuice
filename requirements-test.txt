# Test-only dependencies. Home Assistant itself, aiohttp and every other
# runtime library are pulled in by pytest-homeassistant-custom-component,
# which pins the Home Assistant version the suite runs against.
pytest-homeassistant-custom-component==0.13.*
beautifulsoup4>=4.12.0
