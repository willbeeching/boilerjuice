# Only used by the manual diagnostic scripts in scripts/. The integration
# itself reads its credentials from the Home Assistant config entry, never
# from a file.
BOILERJUICE_EMAIL=your_email@example.com
BOILERJUICE_PASSWORD=your_password
# Optional: pin a specific tank when the account has more than one.
BOILERJUICE_TANK_ID=
